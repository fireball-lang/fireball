struct Point {
	x: i32,
	y: i32,
}

func get() Point {
	var p: Point;
	p.x = 5;
	p.y = 5 * 3;

	return p;
}

func print(p: Point) {
	printf("X: %d, Y: %d\n", p.x, p.y);
}

func main() i32 {
	var p = get();
	print(p);

	return p.y;
}

func printf(format: *u8, ...) void
