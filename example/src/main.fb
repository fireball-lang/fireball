mod example;

import example::math::{get};
import example::math;

func main() i32 {
	var p = get();
	print(p);

	return p.y;
}

func print(p: math::Point) {
	printf("X: %d, Y: %d\n", p.x, p.y);
}

func printf(format: *u8, ...) void
