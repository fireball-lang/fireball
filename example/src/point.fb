mod example::math;

import example::c::{printf};

pub struct Point {
	pub x: i32,
	pub y: i32,
}

impl Point {
	pub func new(x: i32, y: i32) Point {
		var p: Point;

		p.x = x;
		p.y = y;

		return p;
	}

	pub func print(self) {
		printf("X: %d, Y: %d\n".ptr, self.x, self.y);
	}
}
