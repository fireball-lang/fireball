mod example::math;

import example::c::{printf};

struct Point {
	x: i32,
	y: i32,
}

impl Point {
	func new(x: i32, y: i32) Point {
		var p: Point;

		p.x = x;
		p.y = y;

		return p;
	}

	func print(self) {
		printf("X: %d, Y: %d\n", self.x, self.y);
	}
}
