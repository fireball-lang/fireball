name: Build

on:
  - push
  - workflow_dispatch

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact: Windows
            ext: ".exe"
          - os: ubuntu-latest
            artifact: Linux
            ext: ""
          - os: macos-latest
            artifact: macOS
            ext: ""

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version-file: src/go.mod
          cache-dependency-path: src/go.sum

      - name: Build
        run: |
          cd src
          go build -o ../fireball${{ matrix.ext }} ./cmd

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: fireball${{ matrix.ext }}

  test:
    needs: build
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact: Windows
            ext: ".exe"
          - os: ubuntu-latest
            artifact: Linux
            ext: ""
          - os: macos-latest
            artifact: macOS
            ext: ""

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v7

      - name: Download artifact
        uses: actions/download-artifact@v8
        with:
          name: ${{ matrix.artifact }}

      - name: Restore executable bit
        if: runner.os != 'Windows'
        run: chmod +x fireball

      - name: Setup LLVM
        uses: ./.github/actions/setup-llvm
        with:
          version: "22.1.9"

      - name: Run tests
        shell: bash
        run: |
          cd tests
          ../fireball${{ matrix.ext }} test

  package-vscode:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: "26.x"
          cache: npm
          cache-dependency-path: vscode-extension/package-lock.json

      - name: Install VSCE
        run: npm install -g @vscode/vsce

      - name: Build VSCode extension
        run: |
          cd vscode-extension
          npm install
          vsce package

      - name: Upload VSCode extension
        uses: actions/upload-artifact@v7
        with:
          name: VSCode
          path: vscode-extension/*.vsix
