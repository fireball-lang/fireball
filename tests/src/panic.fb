mod tests;

#[extern]
func printf(fmt: *u8, ...);

#[extern]
func exit(code: i32);

#[init]
func init_panic() {
    set_panic_handler(panic_handler);
}

func panic_handler(msg: StringView) {
    printf("panic: %.*s\n".ptr, msg.size, msg.ptr);
    exit(1);
}
